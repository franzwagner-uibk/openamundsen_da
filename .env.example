# Example environment file for Docker Compose.
# Copy this file to ".env" and adjust values for your machine.

# Paths to mount into the container
# Linux/macOS example:
# REPO=/home/you/openamundsen_da
# PROJ=/home/you/projects/rofental

# Windows example (Docker Desktop translates paths automatically):
# REPO=C:\Users\you\openamundsen_da
# PROJ=C:\Users\you\projects\rofental

REPO=/abs/path/to/openamundsen_da
PROJ=/abs/path/to/your/project

# Optional: set container user to match your host (Linux/macOS) to avoid root-owned files
# UID=1000
# GID=1000

# Resource limits (must not exceed your Docker/WSL2 global limits)
CPUS=8
MEMORY=16g
# Optional: cap workers inside the container
# MAX_WORKERS=8

# Notes:
# - Docker Compose automatically reads ".env" from the repo root.
# - Keep ".env" untracked (see .gitignore); only commit ".env.example".
