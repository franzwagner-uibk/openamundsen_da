@AGENTS.md

## Claude Code

- Path-scoped Claude rules live under `.claude/rules/`; treat them as the main Claude-specific scoping layer.
- Use `/memory` if loaded instructions seem incomplete or stale.
- Keep shared project guidance in `AGENTS.md`; keep this file minimal and Claude-specific.
