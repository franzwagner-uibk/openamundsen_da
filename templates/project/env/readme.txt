expected files:
- roi.gpkg: single-feature ROI polygon in model CRS (default area of interest for all operations).
- glaciers.gpkg (optional): glacier outlines; when present and enabled in project.yml, used to mask glaciers from ROI for SCF and wet-snow DA.

